# Build Docsy & the website and run all repo checks across OSs.
# cSpell:ignore docsy

name: test

on:
  push:
    branches: [main]
  pull_request:
  # schedule:
  #   - cron: '11 0 * * *' # 11 past midnight every day
  workflow_dispatch:
permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: package.json

      - run: npm install --omit=optional

      # Install the pinned lychee binary for the link checker, which expects it
      # on PATH. Only the Linux runner runs the online check (ci:test); the
      # lychee-action installer can't handle the versioned tarball layout
      # (binary under lychee-<target>/, not the archive root), so fetch it here.
      - if: runner.os != 'Windows'
        name: Install lychee
        env:
          LYCHEE_VERSION: v0.24.2
        run: |
          tmp="$(mktemp -d)"
          curl -sfL "https://github.com/lycheeverse/lychee/releases/download/lychee-${LYCHEE_VERSION}/lychee-x86_64-unknown-linux-gnu.tar.gz" \
            | tar -xz -C "$tmp"
          install -D "$(find "$tmp" -type f -name lychee | head -n1)" "$HOME/.local/bin/lychee"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - if: runner.os != 'Windows'
        run: npm run ci:test

      - if: runner.os == 'Windows'
        run: npm run ci:prepare && npm run -C docsy.dev build && npm run ci:post

      - uses: actions/upload-artifact@v6
        with:
          name: test-site-${{ matrix.os }}
          path: |
            docsy.dev/public
