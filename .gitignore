node_modules/
# Committed locks; the anchored ! guards against user-global ignore patterns.
!/package-lock.json
!/theme/package-lock.json
/tmp

# Local visual-golden baselines; only linux (authoritative) and darwin are
# committed:
/tests/visual/goldens/*
!/tests/visual/goldens/linux
!/tests/visual/goldens/darwin

/theme/LICENSE

# ScrollSpy patch related
# Temporary files created by the patch command when it fails:
assets/_cache/bootstrap/scrollspy-method.js.*

# Hugo
.hugo_build.lock
/public
resources/

# vim temporary files
*~
*.sw?

# system files
.DS_Store

# IDE files
.idea

# VS Code
/.vscode/**
!/.vscode/cspell.json
/.cursor

# cSpell:ignore worktrees
/.claude/worktrees

# Local folders and settings
/.claude/settings.local.json
