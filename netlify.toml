[build]
  publish = "userguide/public"
  command = "git submodule update -f --init && cd userguide && npm install postcss-cli && hugo --baseURL="https://www.docsy.dev" --themesDir ../.."

[build.environment]
  HUGO_VERSION = "0.55.6"
  HUGO_THEME = "repo"
  HUGO_ENV = "production"
