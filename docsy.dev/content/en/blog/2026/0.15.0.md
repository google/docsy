---
title: Release 0.15.0 report and upgrade guide
linkTitle: Release 0.15.0
date: 2026-04-25
lastmod: 2026-04-25
author: >-
  [Patrice Chalin](https://github.com/chalin) ([CNCF](https://www.cncf.io/)),
  for the [Docsy Steering Committee](/blog/2022/hello/#introducing-the-psc)
body_class: release-highlights
tags: [release, upgrade]
cSpell:ignore: afdocs doc-rooted llms markdownify RenderString
---

<!-- markdownlint-disable no-space-in-emphasis link-image-reference-definitions -->

> [!WARNING] Draft
>
> Before publishing this post, confirm the final Docsy version, supported Hugo
> and Node.js versions, and whether [#2504][] closes for 0.15.0.

{{% card header="Highlights" %}}

- {{% _param FAS_LG robot info %}} <span>**[Agent support](#agent-support)**:
  `llms.txt`, Markdown page output, and a "View Markdown" page-meta link</span>
- {{% _param FAS_LG diagram-project success %}}
  <span>**[Doc-rooted sites](#doc-rooted-sites)**: improved support for
  publishing docs at the site root</span>
- {{% _param FAS_LG triangle-exclamation warning %}}
  <span>**[Action review](#ready-to-upgrade)**: community/footer link paths,
  `card` shortcode rendering, and runtime versions</span>

{{% /card %}}

## Release summary

- **[Doc-rooted sites](#doc-rooted-sites)**: improved support
- **[Agent support](#agent-support)**
- **Content, shortcodes, and internationalization**:
  - [Community and footer links](#community-footer-links)
  - [`card` shortcode rendering](#card-shortcode)
  - [Internationalization](#internationalization) additions and updates

## Ready to Upgrade? <a id="breaking-changes"></a> {#ready-to-upgrade}

Section status: revisited as of 2026-04-25

- Review {{% _param BADGE BREAKING warning %}} changes:
  - {{% _param BREAKING %}}
    [Community and footer links](#community-footer-links)
  - {{% _param BREAKING %}} [Card shortcode rendering](#card-shortcode)
- Optionally skim:
  - {{% _param NEW %}} New features (look for the green checkmark icon)
  - {{% _param CLEANUP %}} Cleanup / improvement opportunities (look for this
    icon)
  - [Other notable changes](#other-notable-changes)
- {{% _param FAS rocket primary %}} Jump to [Upgrade to 0.15.0](#upgrade) once
  you are ready

## {{% _param NEW %}} Agent support {#agent-support}

Docsy 0.15.0 adds [experimental][] support for agent-readable generated content:

- A generated top-level `llms.txt` index.
- Markdown alternate outputs for home, section, and page content.
- A "View Markdown" page-meta link when a Markdown alternate exists.

This feature is opt-in for sites. The first implementation prefers a simple,
stable Markdown representation over reproducing the full visual page chrome. For
details, see [Agent support][].

[Agent support]: /docs/content/agent-support/

### Actions {#agent-support-actions}

{{% _param BADGE EXPERIMENTAL info %}} **Applies if** you want to try agent
support output.

- Enable Markdown outputs only for page kinds where the output is useful.
- Review generated Markdown for pages that depend heavily on shortcodes.
- Treat `llms.txt` as an experimental discovery aid and verify its links before
  publishing it on your production site.

## Doc-rooted sites {#doc-rooted-sites}

Section status: done as of 2026-04-25

Docsy provides new and improved support for _doc-rooted_ sites. That is, sites
whose `docs` section is published at the site root instead of under `/docs/`.
This is useful when your site is primarily documentation and you want shorter
URLs such as `/get-started/` instead of `/docs/get-started/`.

Doc-rooted sites also support having non-docs sections at the site root, such as
a blog or community page. For details, see [Doc-rooted sites][]. For an example,
see the [Doc-rooted example][] variant of this site. This feature is
[experimental](#experimental).

### Actions {#doc-rooted-sites-actions}

{{% _param CLEANUP %}} **Applies if** your project uses an older docs-only
configuration based on front matter `cascade` or `type` changes.

- Remove the older docs-only cascade configuration.
- Use Hugo `permalinks` configuration for the `docs` section.
- Check for path conflicts with `hugo --printPathWarnings`.

## {{% _param BREAKING %}} / {{% _param NEW %}} Community and footer links {#community-footer-links}

Section status: done as of 2026-04-25

New behavior:

- Community and footer links support `rel` attributes ([#2576][]).
- Site-local community and footer links resolve under any [permalinks][] scheme
  ([#2580][]).

[permalinks]: https://gohugo.io/configuration/permalinks/

Breaking changes:

- Community and footer links only open in a new target for external links
  ([#2576][]). Previously, site-local links opened in a new target as well.
- For multilingual sites, link paths are now interpreted as site-relative paths.
  ([#2580][]).

### Actions {#community-footer-links-actions}

{{% _param BREAKING %}} **Applies if** your multilingual site configures
community or footer links with site-local paths.

- Review `params.links.user` and `params.links.developer` path values. For each,
  determine whether the path should target the default language or be site
  (locale) relative.
- If the path should be site-relative, leave the path as is.
- If the path should target the default language site, prefix the path with the
  default language code, for example use `/en/community/` instead of
  `/community/`.
- Recheck generated community and footer links in each language to ensure they
  target the correct site.

For the configuration shape, see [Adding a community page][].

## {{% _param BREAKING %}} Card shortcode rendering {#card-shortcode}

Docsy 0.15.0 changes how the [`card` shortcode][] renders its `header`, `title`,
`subtitle`, and `footer` fields. These fields now use `$.Page.RenderString`
instead of `markdownify`, so they render in the page context ([#2565][]).

For most sites, this should make rendering more correct. It may affect projects
that override the shortcode or depend on the previous rendering behavior.

### Actions {#card-shortcode-actions}

{{% _param BREAKING %}} **Applies if** your project uses `card` shortcode
parameters with Markdown, images, links, or custom shortcode overrides.

- Review rendered cards that use `header`, `title`, `subtitle`, or `footer`.
- Check any local `layouts/_shortcodes/card.html` override against the upstream
  template.

## Other notable changes {#other-notable-changes}

### {{% _param FAS globe "" %}} Internationalization {#internationalization}

Section status: done as of 2026-04-25

Summary of changes:

- Translation files for the following locales have been created or updated:
  - Azerbaijan: new ([#2082][], [#2604][]).
  - Romanian: new ([#2583][], [#2603][]).
  - German: added alert-label translations ([#2591][]).
- Baseline translations for the new "View Markdown" label ([#2602][]).

### Documentation and release process

Docsy 0.15.0 also includes documentation and project-process updates:

- Deployment docs are split into provider-specific pages ([#2556][]).
- The branch model documents `main`, `release`, `deploy/prod`, and `doc-rooted`
  ([#2572][]).
- Link linting now discourages full `docsy.dev` URLs for site-local links
  ([#2584][]).

## {{% _param FAS rocket primary %}} Upgrade to 0.15.0 {#upgrade}

Some upgrade steps are the same for each Docsy release. Follow the general
module-update flow from the previous release report, using version **0.15.0**
where that guide references 0.14.x.

For this release, review:

- **Docsy**: 0.14.3 → 0.15.0.
- **Hugo**: confirm the final supported Hugo version before publishing. The
  current `docsy.dev` tooling uses `hugo-extended` 0.157.0.
- **Node.js**: confirm the final supported Node.js range before publishing.

### Sanity checks

- Build your site locally with the final supported Hugo version.
- Check multilingual community and footer links.
- Check pages that use the `card` shortcode.
- If you enable Markdown outputs or `llms.txt`, inspect the generated `.md`
  pages and `/llms.txt`.
- If you use a doc-rooted configuration, run Hugo with `--printPathWarnings`.

## What's next?

Agent support remains experimental. Follow [#2596][] for continued work on
generated Markdown, `llms.txt`, and related docs tooling.

For doc-rooted site follow-up, see [#2504][].

## References

About this release:

- Changelog entry for [0.15.0][CL@0.15.0]
- Release page for [0.15.0][]
- [Release 0.15.0 preparation issue (#2501)][#2501]

Related documentation:

- [Doc-rooted sites][]
- [Adding a community page][]
- [`card` shortcode][]
- [Upgrade notes for 0.14.0][0.14.0-upgrade]

[#2082]: https://github.com/google/docsy/pull/2082
[#2501]: https://github.com/google/docsy/issues/2501
[#2504]: https://github.com/google/docsy/issues/2504
[#2556]: https://github.com/google/docsy/pull/2556
[#2565]: https://github.com/google/docsy/pull/2565
[#2572]: https://github.com/google/docsy/pull/2572
[#2576]: https://github.com/google/docsy/pull/2576
[#2580]: https://github.com/google/docsy/pull/2580
[#2583]: https://github.com/google/docsy/pull/2583
[#2584]: https://github.com/google/docsy/pull/2584
[#2591]: https://github.com/google/docsy/pull/2591
[#2596]: https://github.com/google/docsy/issues/2596
[#2602]: https://github.com/google/docsy/pull/2602
[#2603]: https://github.com/google/docsy/pull/2603
[#2604]: https://github.com/google/docsy/pull/2604
[`card` shortcode]: /docs/content/shortcodes/#shortcode-card-textual-content
[0.14.0-upgrade]: /blog/2026/0.14.0/#upgrade
[0.15.0]: https://github.com/google/docsy/releases/latest?FIXME=v0.15.0
[Adding a community page]: /docs/content/adding-content/#adding-a-community-page
[CL@0.15.0]: /project/about/changelog/#v0.15.0
[Doc-rooted example]: https://doc-rooted--docsydocs.netlify.app
[Doc-rooted sites]: /docs/content/adding-content/#doc-rooted-sites
[experimental]: /project/about/changelog/#experimental
