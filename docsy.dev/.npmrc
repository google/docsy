# One script interpreter on every platform; rationale and audit: the root
# .npmrc and tests/lock-audit.test.mjs. Repeated here because a
# workspace-directed run (npm -C docsy.dev ...) reads only this file.
script-shell=bash
